import { Logo } from '@/assets';
import { HeaderContainer } from './styled';

const Header = () => {
  return (
    <HeaderContainer>
      <Logo />
    </HeaderContainer>
  );
};

export default Header;
